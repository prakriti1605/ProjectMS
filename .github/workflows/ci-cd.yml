name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # ── JOB 1: Dependency & Build Validation ──
  validate-and-build:
    name: Code & Build Validation
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Setup Node.js runtime (v18)
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'
          cache-dependency-path: |
            server/package-lock.json
            client/package-lock.json

      # 1. Validate Backend Syntax & Dependencies
      - name: Install & Validate Backend
        run: |
          cd server
          npm ci
          node -e "console.log('Backend syntax validated successfully');"

      # 2. Validate & Build Frontend Production Bundle
      - name: Install & Build Frontend
        env:
          VITE_API_URL: https://project-mgmnt-eu2f.onrender.com/api
        run: |
          cd client
          npm ci
          npm run build

  # ── JOB 2: Docker Container Build Test ──
  docker-validation:
    name: Docker Container Image Validation
    needs: validate-and-build
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: actions/setup-buildx-action@v3

      - name: Test Build Backend Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./server
          file: ./server/Dockerfile
          push: false
          tags: trackit-backend:test

      - name: Test Build Frontend Docker Image
        uses: docker/build-push-action@v5
        with:
          context: ./client
          file: ./client/Dockerfile
          build-args: |
            VITE_API_URL=https://project-mgmnt-eu2f.onrender.com/api
          push: false
          tags: trackit-frontend:test